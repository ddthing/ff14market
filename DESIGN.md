# FF14Market Design System

> FF14Market의 UI, 데이터 표현, 브랜드 아이콘을 위한 공통 기준입니다.
> 이 문서는 모든 화면을 같은 모양으로 만드는 규칙이 아니라, 서비스가 일관되게 보이도록 판단 기준과 허용 범위를 정의합니다.

## 1. 제품의 방향

FF14Market은 파이널판타지14 한국 데이터센터의 장터 데이터를 빠르고 투명하게 읽는 **작고 신뢰할 수 있는 마켓 터미널**입니다.

- **핵심 인상:** 차분함, 정확함, 실용성, 가벼운 친근함
- **지향점:** 증권사 시세 화면처럼 정보의 우선순위가 명확하되, 게임 서비스에 맞는 따뜻함을 유지합니다.
- **비지향점:** 게임 UI를 그대로 복제하는 장식성, 과도한 게이미피케이션, 장식 때문에 데이터가 묻히는 화면
- **우선순위:** 의미 전달과 접근성 → 데이터 신뢰성 → 탐색 속도 → 브랜드 개성

표현은 차분하게 유지하되, 한 화면이나 아이콘마다 작은 개성은 허용합니다. 일관성은 모든 요소를 똑같이 만드는 것이 아니라, 같은 원칙 안에서 변주하는 것으로 정의합니다.

## 2. 적용 범위와 기준 우선순위

### 적용 범위

1. 제품 UI: 검색, 목록, 순위, 상세, 모달, 로딩·오류·빈 상태
2. 데이터 표현: 가격, 거래량, 가격 변동, 최신성, 계산 기준
3. 반응형 레이아웃과 인터랙션
4. 브랜드 자산: 파비콘, PWA 아이콘, 마켓 보드·chibi SD 일러스트
5. 접근성·콘텐츠 표현

### 충돌 시 우선순위

1. 기능의 정확성, 접근성, 키보드 사용성
2. 수치와 상태의 명확한 전달
3. 이 문서의 시맨틱 토큰과 컴포넌트 규칙
4. 화면별 정보 구조
5. 장식과 개성

장식이 수치나 상태를 가리거나, 작은 화면에서 조작을 어렵게 만들면 장식을 줄입니다.

## 3. 현재 기술 기준

저장소의 `components.json`을 UI 시스템의 기준으로 삼습니다.

- Style: `base-vega`
- Color: `neutral`
- Icon target: `Remix Icon`
- Layout: mobile-first
- UI library direction: shadcn/ui의 시맨틱 토큰과 상태 중심 컴포넌트
- Typography: `Pretendard Variable`

현재 기존 컴포넌트에는 `lucide-react` 아이콘이 남아 있을 수 있습니다. 이 문서를 추가하는 작업에서 일괄 교체하지는 않습니다. 새 아이콘을 추가하거나 아이콘을 정리하는 별도 작업에서는 Remix Icon 전환 여부와 패키지 설치 상태를 먼저 확인합니다.

코드에서는 가능한 한 원시 색상과 임의의 값을 직접 쓰지 말고 `--app-*` 및 shadcn 시맨틱 변수를 사용합니다.

## 4. 시각 언어

### 4.1 표면과 선

- 화면 배경은 중성적인 캔버스를 기본으로 사용합니다.
- 카드와 입력 영역은 배경 대비와 얇은 hairline border로 구분합니다.
- 그림자는 깊이를 설명할 때만 작고 부드럽게 사용합니다.
- 중첩된 카드 안에 카드를 반복해 정보가 액자처럼 보이지 않게 합니다.
- 선은 순수 검정 대신 따뜻한 차콜 또는 코코아 계열을 선호합니다.
- 장식용 테두리는 선택사항입니다. 정보 구조를 설명하지 못하면 제거합니다.

### 4.2 색상 역할

색상 이름이 아니라 역할을 기준으로 사용합니다. 아래 팔레트는 브랜드·일러스트의 기준 색이며, 실제 UI에서는 기존 neutral 시맨틱 토큰과 대비 기준을 우선합니다.

| 역할 | 기준 색 | 사용 원칙 |
| --- | --- | --- |
| Canvas | neutral / warm ivory `#F6EBDD` | 넓은 배경, 아이콘 배경 |
| Surface | `--app-surface` | 카드, 패널, 입력 영역 |
| Surface subtle | `--app-surface-subtle` | 보조 영역, 빈 상태, 필터 |
| Ink | warm charcoal `#292625` | 본문과 핵심 수치 |
| Ink muted | neutral gray | 보조 설명, 메타데이터 |
| Hairline | low-contrast neutral | 구획과 입력 경계 |
| Cocoa line | `#5A352A` | 브랜드 일러스트의 선과 핀 |
| Board green | `#30382E` | 마켓 보드 아이콘의 면 |
| Brick accent | `#A64A36` | 제한적인 강조와 상태 보조 |
| Ochre accent | `#C9974A` | 카드, 보조 강조, 핀 |
| Plum accent | `#8A1F5B` | 캐릭터 눈 또는 브랜드 포인트에 제한적으로 사용 |

#### 색상 사용 규칙

- 색상만으로 상승·하락·오류를 구분하지 않습니다. 부호, 라벨, 아이콘, 설명을 함께 제공합니다.
- 시장 데이터의 방향성은 현재 서비스의 관례를 유지합니다: 상승은 따뜻한 적색 계열, 하락은 청색 계열. 단, 색상은 반드시 텍스트와 함께 사용합니다.
- 자주색은 모든 강조에 쓰지 않습니다. 브랜드 포인트나 선택 상태처럼 좁은 범위에만 사용합니다.
- 검정에 가까운 배경과 선을 남발하지 않습니다. 다크 모드에서도 텍스트와 경계의 대비를 먼저 확인합니다.
- 새 색상을 추가하기 전에 기존 시맨틱 토큰으로 표현할 수 있는지 확인합니다.

### 4.3 모서리, 밀도, 깊이

- 기본 형태는 둥글고 간결하게 유지합니다.
- 모서리 반경은 컴포넌트 종류별로 반복 사용하고, 컴포넌트마다 임의로 바꾸지 않습니다.
- 버튼과 입력은 모바일에서 최소 44px의 터치 영역을 확보합니다.
- 목록은 정보 밀도를 높이되, 이름·가격·상태가 한 덩어리로 뭉치지 않도록 2~3단계로 나눕니다.
- 헤더와 푸터는 콘텐츠보다 존재감이 작아야 합니다. 탐색에 필요한 높이만 사용합니다.
- 애니메이션, 그림자, 그라디언트는 상태 변화나 계층 설명에 필요할 때만 사용합니다.

## 5. 타이포그래피와 콘텐츠

- 기본 글꼴은 `Pretendard Variable`을 사용합니다.
- 화면 제목은 짧고 명확하게 작성하며, 한 화면에 가장 강한 제목은 하나만 둡니다.
- 핵심 수치에는 `font-variant-numeric: tabular-nums`에 해당하는 정렬 특성을 사용합니다.
- 단위는 숫자와 붙여 읽을 수 있게 표시하고, 보조 설명은 한 단계 작은 크기와 낮은 대비로 구분합니다.
- 순위나 신호를 표시할 때는 “무엇을 기준으로 정렬했는지”와 “어느 기간을 비교했는지”를 가까운 위치에 함께 보여줍니다.
- 계산식, 기준 시점, 데이터 최신성은 숨기지 않습니다. 사용자가 결과를 검증할 수 있어야 합니다.
- 버튼과 링크는 아이콘만으로 의미를 전달하지 말고 accessible name을 제공합니다.
- 오류와 빈 상태는 원인, 현재 상태, 다음 행동을 짧게 설명합니다.

## 6. 정보 구조와 주요 화면

### 홈

- 검색을 첫 번째 행동으로 둡니다.
- 추천 검색어는 보조 행동으로 보이게 하고, 검색창보다 시각적 우선순위를 높이지 않습니다.
- 관심 아이템과 최근 본 아이템은 사용자의 맥락을 유지하는 보조 섹션입니다.

### 장터 신호

- `거래량 급증`, `가격 하락 신호`, `최저 매물가 TOP`은 탭 또는 명확한 필터로 구분합니다.
- 활성 탭만 강하게 강조하고, 비활성 탭은 정보 패널과 혼동되지 않게 합니다.
- 패널 상단에 기간, 계산 기준, 현재 표시 개수를 노출합니다.
- 이력 비교가 오래 걸릴 때는 현재 지표를 먼저 보여주되, 임시 지표임을 명시하고 최종 기준으로 자동 전환되는 상태를 설명합니다.
- 순위 행에서는 아이템명, 현재 가격, 보조 지표, 최신성을 한눈에 비교할 수 있어야 합니다.

### 아이템 상세

- 최저 가격과 업데이트 시점을 상단에서 먼저 보여줍니다.
- 가격 흐름은 장식이 아니라 추세 비교를 위한 보조 정보입니다.
- 서버별 비교에서는 최저 서버를 색상만이 아닌 라벨로도 표시합니다.
- 외부 API 데이터의 지연이나 인게임과의 차이는 설명 가능한 위치에 둡니다.

### 반응형

- 모바일을 기본으로 설계하고, 데스크톱에서는 폭과 여백을 확장합니다.
- 모바일 하단 내비게이션은 핵심 3개 이하의 목적만 유지합니다.
- 데스크톱 전용 hover 정보가 모바일의 필수 기능이 되지 않게 합니다.
- 긴 아이템명과 수치는 잘림보다 우선순위가 분명한 축약·줄바꿈을 선택합니다.

## 7. 아이콘과 일러스트

### 7.1 제품 UI 아이콘

- 일반 UI 아이콘의 목표 라이브러리는 Remix Icon입니다.
- 한 화면 안에서 아이콘의 선 굵기, 시각적 크기, 정렬 기준을 통일합니다.
- 아이콘은 텍스트를 보조하며, 핵심 기능을 아이콘만으로 표현하지 않습니다.
- 기능과 직접 관련 없는 이모지는 장식으로만 사용하고, 상태·경고·동작을 이모지 하나에 맡기지 않습니다.
- 아이콘 버튼에는 hover, focus-visible, disabled 상태를 모두 정의합니다.

### 7.2 브랜드 아이콘과 chibi SD 규칙

앱 아이콘과 파비콘은 제품 UI 아이콘과 다른 브랜드 자산입니다. Remix Icon의 기하학을 그대로 강제하지 않고, 아래의 스타일 DNA만 공유합니다.

**고정할 기준**

- 한눈에 읽히는 단일 실루엣
- 2~4개의 큰 색면 중심
- 둥글고 짧은 chibi SD 비례
- 따뜻한 아이보리, 코코아 브라운, 중성 녹색, 벽돌색, 제한적인 자주색
- 검은 외곽선, 포토리얼 질감, 광택 3D, 미세한 장식과 텍스트 금지
- 16px, 32px, 64px에서 핵심 형태가 유지되어야 함

**유연하게 둘 기준**

- 테두리의 유무와 두께
- 비대칭, 카드의 기울기와 겹침
- chibi SD의 귀여운 정도
- 배경의 여백과 모서리 처리
- 아이콘별 핵심 오브젝트의 개수

**마켓 보드 방향**

- 원본 게임 스크린샷의 복잡한 목재 프레임을 복제하지 않습니다.
- 큰 둥근 게시판 면, 3개 안팎의 공고 카드, 단순한 핀만으로 시장 게시판의 의미를 전달합니다.
- 프레임은 선택사항이며, 실루엣과 공고 카드가 이미 충분히 읽히면 제거합니다.
- 카드 안의 선은 글자를 흉내 내는 디테일이 아니라 짧은 그래픽 마크로만 사용합니다.
- 최종 아이콘 후보는 실제 파비콘 크기에서 먼저 검토한 뒤 PWA 아이콘으로 적용합니다.

생성형 이미지 시안은 방향 탐색용입니다. 최종 적용 전에는 작은 크기에서의 형태, 투명·불투명 배경, 브라우저 마스크, 색상 대비를 별도로 검수합니다.

## 8. 인터랙션과 모션

- 기본 전환은 150~250ms의 짧고 부드러운 페이드·스케일·색상 변화로 제한합니다.
- 데이터 로딩은 레이아웃을 갑자기 밀어내지 않는 skeleton 또는 현재 콘텐츠 위의 명확한 상태로 표현합니다.
- 느린 이력 비교처럼 사용자가 기다려야 하는 작업은 무엇을 하고 있는지, 임시 결과인지, 완료 후 어떻게 바뀌는지 설명합니다.
- hover는 데스크톱 보조 신호이고, focus-visible은 키보드 사용자를 위한 필수 상태입니다.
- `prefers-reduced-motion: reduce` 환경에서는 장식 애니메이션을 줄이고 상태 변화는 즉시 보여줍니다.

## 9. 접근성 품질 기준

- 텍스트와 배경은 WCAG 대비 기준을 만족하도록 확인합니다.
- 색상만으로 상태를 구분하지 않습니다.
- 모든 폼 요소에 label 또는 accessible name을 제공합니다.
- 탭, 모달, 드롭다운은 키보드로 열고 이동하고 닫을 수 있어야 합니다.
- 이미지에는 목적에 맞는 alt를 제공합니다. 장식 이미지는 보조기술에서 숨깁니다.
- 포커스 링을 제거하지 않습니다. 배경과 충분히 대비되는 focus-visible 상태를 사용합니다.
- 작은 화면에서도 수치와 단위가 겹치지 않도록 실제 콘텐츠로 테스트합니다.

## 10. SEO와 신뢰 콘텐츠

SEO는 장식이 아니라 정보 구조의 확장입니다.

- 공개 정보 화면은 하나의 명확한 H1과 설명적인 page title·description을 가집니다.
- 검색 엔진용 텍스트에도 실제 화면의 핵심 의미와 계산 기준이 반영되어야 합니다.
- 이용약관과 개인정보처리방침은 사용자가 쉽게 접근할 수 있게 유지하고, 개인화 화면과 함께 검색 노출 정책을 명시적으로 관리합니다.
- 광고·후원·외부 데이터 출처는 콘텐츠와 시각적으로 구분합니다.
- Universalis 데이터의 출처와 최신성, 잠재적인 지연을 사용자에게 설명합니다.

## 11. 새 화면·컴포넌트 체크리스트

작업을 시작하기 전에 다음을 확인합니다.

- 이 화면의 가장 중요한 한 가지 행동과 한 가지 정보는 무엇인가?
- 기존 `--app-*` 또는 shadcn 시맨틱 토큰으로 표현할 수 있는가?
- 모바일에서 44px 터치 영역과 가독성을 확보했는가?
- 로딩, 오류, 빈 상태, 오래된 데이터 상태를 정의했는가?
- 수치의 단위, 기간, 계산 기준을 가까운 위치에 표시했는가?
- 색상 없이도 상승·하락·선택·오류를 이해할 수 있는가?
- 새 아이콘이 Remix Icon 기준과 충돌하지 않는가?
- 브랜드 자산이라면 16/32/64px에서 실루엣을 확인했는가?
- 장식 요소를 제거해도 정보가 유지되는가?
- 키보드, reduced motion, 다크 모드에서 확인했는가?

## 12. 금지 패턴

- 작은 화면을 고려하지 않은 데스크톱 전용 표와 hover 의존 인터랙션
- 의미 없는 그라디언트, 유리 효과, 과한 그림자와 blur
- 카드 안에 또 다른 카드가 반복되는 과도한 계층
- 검은 외곽선과 세밀한 게임풍 프레임을 모든 브랜드 아이콘에 강제
- AI 생성 이미지의 미세한 질감, 불규칙한 손·문자·장식물을 최종 자산으로 그대로 사용
- 숫자나 상태를 색상만으로 전달
- 실제 계산 기준과 다른 마케팅성 문구
- 새 컴포넌트마다 새로운 색상·radius·spacing 값을 임의로 추가

## 13. 변경 원칙

디자인 변경은 다음 순서로 진행합니다.

1. 정보 구조와 상태를 먼저 정의합니다.
2. 기존 토큰과 컴포넌트로 재사용 가능한지 확인합니다.
3. 필요한 경우에만 시맨틱 토큰을 추가합니다.
4. UI와 브랜드 자산의 규칙을 분리해 기록합니다.
5. 모바일·다크 모드·접근성·실제 데이터로 검증합니다.
6. 새로운 패턴이 반복될 때만 이 문서에 일반 규칙으로 승격합니다.

이 문서에 아직 기록되지 않은 표현은 금지된 것이 아닙니다. 먼저 작은 범위에서 검증하고, 제품 전반에 반복해서 사용할 가치가 확인될 때 기준으로 추가합니다.
